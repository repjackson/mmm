template(name='register')
    .ui.padded.stackable.grid
        .centered.row
            .ten.wide.column
                .ui.center.aligned.basic.segment
                    img.ui.rounded.small.centered.inline.image(src="{{c.url 'mmmlogo' width=400 height=400}}")
                    //- +i name='add-user-male' classes='ui mini inline image'
                    .ui.inline.header registration
                if message
                    h4.ui.center.aligned.header #{message}
                .ui.small.inline.header 
                    //- +i name='user-male'
                    i.user.grey.icon
                    |username
                .ui.fluid.input(class=login_class)
                    input.username(type='text')
                .ui.small.inline.header 
                    //- +i name='password'
                    i.lock.grey.icon
                    | password
                .ui.fluid.input(class=login_class)
                    input.password(type='password')
                //- .ui.fluid.labeled.input(class=login_class)
                //-     .ui.small.inline.header 
                //-         i.lock.grey.icon
                //-         | password again
                //-     input.password2(type='password')
                if username
                    if registering
                        .ui.fluid.large.circular.button.enter(class=enter_class)
                            +i name='enter-2' classes='ui mini inline image'  
                            |register '#{username}'
                    else
                        .ui.small.header username "#{username}" found
                        a.ui.basic.fluid.button(href='/login') 
                            +i name='enter-2' classes='ui mini inline image'                    
                            |login
                        
                        
                .ui.center.aligned.basic.segment    
                    .ui.small.header by registering you agree to our 
                        a(href='/p/privacy' target="_window") privacy policy
                    unless username
                        .ui.small.header or
                            a.ui.circular.basic.sign.in.button(href='/login') 
                                +i name='enter-2' classes='ui mini inline image'                    
                                |login
                    
template(name='choose_personas')
    .ui.padded.stackable.grid
        .centered.row
            .twelve.wide.column
                .ui.center.aligned.basic.segment
                    img.ui.rounded.small.centered.inline.image(src="{{c.url 'mmmlogo' width=400 height=400}}")
                    //- +i name='add-user-male' classes='ui mini inline image'
                    //- .ui.inline.header registration
                    div
                    .ui.huge.header I am a...
                    each user_personas
                        .ui.large.button.select_persona(class=persona_class)
                            +i name=icon classes='ui inline avatar image'
                            |#{title}
                    .spacer
                    a.ui.big.green.button(href="/choose_interests")
                        |continue
                        i.right.chevron.icon
                            
                            
template(name='choose_interests')
    .ui.padded.stackable.grid
        .centered.row
            .twelve.wide.column
                .ui.center.aligned.basic.segment
                    img.ui.rounded.small.centered.inline.image(src="{{c.url 'mmmlogo' width=400 height=400}}")
                    //- +i name='add-user-male' classes='ui mini inline image'
                    //- .ui.inline.header registration
                    div
                    .ui.huge.header I want to...
                    each user_interests
                        .ui.large.button.select_interest(class=interest_class)
                            +i name=icon classes='ui inline avatar image'
                            |#{title}
                    .spacer
                    a.ui.button(href="/choose_personas")
                        i.left.chevron.icon
                        |back
                    a.ui.big.green.button(href="/user/#{currentUser.username}")
                        |continue
                        i.right.chevron.icon
